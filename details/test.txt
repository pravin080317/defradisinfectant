 if ($Rules.ContainsKey("values") -and $Rules.values -is [array] -and $Value -and (-not ($Rules.values -contains $Value))) {
                $Errors += "$Column must be one of: $($Rules.values -join ', ')"
            }
